node_modules/
build/
dist/
coverage/
.DS_Store
*.log

# Secrets — never commit. Only the template is tracked.
.env*
!.env.example

# Scratch build output from the production-readiness audit.
# Safe to delete these directories entirely.
.apirun/
.apitest/
.apitest2/
preview-build/

# Leftover scratch file from the audit (sandbox could not delete it).
frontend/src/__eb_test.tsx
.diag*/
.diag-ui.html
.rbac*/
.sa/
.dbtest.mjs
.rlstest.mjs
.rlsneg.mjs
.dbcheck.mjs
.pt/
.pv/
# Throwaway verification probe; the sandbox could not unlink it. Safe to delete.
_probe.mjs
